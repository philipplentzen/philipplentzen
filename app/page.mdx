import { Subtitle } from "@/components/ui/subtitle";

<div className={`mb-4 flex items-center`}>
  <div className={`relative mr-1 inline-flex size-2`}>
    <span
      className={`absolute inline-flex size-full animate-ping rounded-full bg-cyan opacity-75`}
    ></span>
    <span
      className={`relative inline-flex size-full rounded-full bg-cyan`}
    ></span>
  </div>
  <Subtitle className={`text-xs`}>verfügbar</Subtitle>
</div>

# Philipp Lentzen

<div>
  {[`Kreativer Entwickler`, `Informatikstudent`, `Öcher Jong`].map(
    (text, index) => (
      <Subtitle key={index} className={`font-mono text-2xl lowercase `}>
        {text}.
      </Subtitle>
    )
  )}
</div>
